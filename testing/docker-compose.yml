version: "3.9"
services:
  selenium:
    build: .
    volumes:
      - .:/app/code
      - wordle_test_reports:/reports
    command: pytest --verbose test_game.py -x --pdb --junitxml=/reports/wordle_test_report.xml

volumes:
  wordle_test_reports: